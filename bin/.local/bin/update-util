#!/bin/sh

set -x

brew update
brew upgrade
brew cleanup
brew autoremove

rustup update
cargo install-update -a

bob use latest
nvim --headless "+Lazy! sync" +qa

softwareupdate --download --recommended
