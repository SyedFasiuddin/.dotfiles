live_config_reload: true

window:
  dimensions:
    columns: 180
    lines: 60
  opacity: 0.8
  padding:
    x: 12
    y: 12
  dynamic_padding: true
  startup_mode: Windowed              # macOS only
  decorations: buttonless             # macOS only
  #decorations: none                  # for others only

scrolling:
  history: 10000
  multiplier: 3

env:
  TERM: xterm-256color

shell:
#  program: cmd 
  program: /opt/homebrew/bin/tmux
  args:
    - new
    - -A
    - -s
    - MASTER

font:
  normal:
    family: "SFMono Nerd Font"
    style: Regular
  bold:
    family: "SFMono Nerd Font"
    style: Bold 
  size: 12.5

draw_bold_text_with_bright_colors: true

cursor:
  style: Block
  blinking: Always
  unfocused_hollow: true
  # hide_when_typing: true

mouse:
  hide_when_typing: true

key_bindings:
    - { key: Return,   mods: Command,       action: ToggleFullscreen             }
  #  - { key: Equals,   mods: Control,       action: IncreaseFontSize             }
  #  - { key: Minus,    mods: Control,       action: DecreaseFontSize             }
  #  - { key: Q,        mods: Alt,           action: Quit                         }


#######################################
##      START OF COLOR SCHEMES       ##
#######################################

# Dim colors
#
# If the dim colors are not set, they will be calculated automatically based
# on the `normal` colors.

######################################################################
## SET THEME: Choose ONE color scheme from those in the below list. ##
## ###################################################################
# Available themes are:

import:
  - ~/.config/alacritty/colorschemes/ayu-dark.yml
# - ~/.config/alacritty/colorschemes/catppuccin-frappe.yml
# - ~/.config/alacritty/colorschemes/catppuccin-macchiato.yml
# - ~/.config/alacritty/colorschemes/catppuccin-mocha.yml
# - ~/.config/alacritty/colorschemes/doom-one.yml
# - ~/.config/alacritty/colorschemes/dracula.yml
# - ~/.config/alacritty/colorschemes/github-dark.yml
# - ~/.config/alacritty/colorschemes/gruvbox-powerline.yml
# - ~/.config/alacritty/colorschemes/gruvbox.yml
# - ~/.config/alacritty/colorschemes/monokai.yml
# - ~/.config/alacritty/colorschemes/moonfly.yml
# - ~/.config/alacritty/colorschemes/nord.yml
# - ~/.config/alacritty/colorschemes/oceanic-next.yml
# - ~/.config/alacritty/colorschemes/onedark.yml
# - ~/.config/alacritty/colorschemes/papercolor.yml
# - ~/.config/alacritty/colorschemes/solarized-dark.yml
# - ~/.config/alacritty/colorschemes/solarized-light.yml
# - ~/.config/alacritty/colorschemes/sonokai.yml
# - ~/.config/alacritty/colorschemes/tokyonight-storm.yml
# - ~/.config/alacritty/colorschemes/tokyonight.yml
# - ~/.config/alacritty/colorschemes/tomorrow-night.yml

#######################################
##       END OF COLOR SCHEMES        ##
#######################################
